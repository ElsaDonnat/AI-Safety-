import { Home, BookOpen, Library, RefreshCw, Trophy } from 'lucide-react';

// Navigation icons using Lucide React for a clean, consistent look.
const SIZE = 20;

export const NAV_ICONS = {
    home: <Home size={SIZE} />,
    learn: <BookOpen size={SIZE} />,
    library: <Library size={SIZE} />,
    practice: <RefreshCw size={SIZE} />,
    challenge: <Trophy size={SIZE} />,
};
